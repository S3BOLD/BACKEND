# Personal Expenses API

## Descrição
API REST para gerenciamento de despesas pessoais.

## Como executar
npm install
npm start

## Rotas
GET /expenses
GET /expenses/:id
POST /expenses
PUT /expenses/:id
DELETE /expenses/:id
GET /expenses/summary/total
GET /expenses/summary/category
